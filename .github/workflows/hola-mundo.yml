name: Hola Mundo

on:
  push:
    branches:
      - aprendiendo-github-actions

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
      - run: node main.js